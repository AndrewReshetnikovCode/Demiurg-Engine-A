// =============================================================================
// vk_window.cpp — superseded by render_graph.cpp (Phase 3). Kept as a
// placeholder so the CMake target references a file if any external tooling
// was already reading it. Real entry point lives next door.
// =============================================================================
#include "demen/renderer.hpp"

namespace demen::renderer { /* intentionally empty */ }
